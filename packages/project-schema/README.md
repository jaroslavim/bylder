# @bylder/project-schema

Versioned project file format (zod schemas + TS types), shared by `apps/web` and (later) `apps/api`. Owned by the `schema-export` agent. See [WORKPLAN.md](../../WORKPLAN.md).
