# @bylder/pdf-export

Installation documentation/report generation (heat-loss sheets, loop layouts, circuit schedules) built from the validated project data model. Owned by the `schema-export` agent. See [WORKPLAN.md](../../WORKPLAN.md).
