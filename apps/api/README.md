# @bylder/api

Phase 5 backend (auth, project CRUD, Postgres storage) — not started yet. See "Backend & API future-proofing" in [WORKPLAN.md](../../WORKPLAN.md) for the contracts the frontend is already built against.
