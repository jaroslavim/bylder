{
  "name": "bylder",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter @bylder/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "e2e": "pnpm --filter @bylder/web exec playwright test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "eslint": "^9.9.0",
    "typescript-eslint": "^8.0.0",
    "prettier": "^3.3.0"
  }
}
